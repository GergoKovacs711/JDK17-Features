package com.zeiss.gergo.kovacs.dojo.challenges;

import com.zeiss.gergo.kovacs.dojo.BasicFunctionalities;

public class SealedTypesChallenges implements BasicFunctionalities {
    /**
     * Create a sealed class hierarchy of you liking
     *
     * Notes:
     * -> Sealed types must be in the same named module (they can be located in a different folder)
     * -> Must define how they are being
     * -> Must directly extend/implement the type
     * -> Works best with APIs
     * -> Records can only use interfaces
     */
}